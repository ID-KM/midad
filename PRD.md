Product Requirements Document (PRD)

1. Project Overview & Identity

  - App Name / Concept: قارئ الكتب المحلي والمنقب المباشر (Flutter
    Offline/Online PDF & EPUB Reader)
  - Color Palette & Visual Style:
      - Primary Light Brown: #8B5A2B (بني فاتح دافئ)
      - Primary Dark Brown: #3E2723 (بني داكن)
      - Accent Yellow/Gold: #F5D061 (أصفر دافئ للتمبير والأيقونات)
      - Background Light: #FDFBF7 (خلفية ورقية دافئة)
      - Background Dark: #121212 (وضع ليلي قياسي)
      - Background AMOLED: #000000 (أسود مطلق للحفاظ على البطارية)
  - Dark Mode / Theming Requirements:
      - دعم الانتقال بين الوضع الفاتح، الوضع الداكن، ووضع AMOLED.
      - دعم وضع "حماية العين" (Eye-Care Mode) الذي يضيف طبقة فلتر دافئة (Sepia
        Tint) قابلة للتعديل.
  - Iconography Rules:
      - يمنع منعاً باتاً استخدام الإيموجي في أي عنصر من عناصر الواجهة (UI).
      - استخدام مكتبة رسمية للأيقونات مثل lucide_icons أو Material Icons.

2. Navigation & Layout

  - Top Bar (الشريط العلوي):
      - حقل بحث عن الكتب عبر الإنترنت (Internet Archive) مع خيار تصفية تلقائي.
  - Screen Structure (شريط التنقل السفلي - Bottom Navigation Bar):
    1.  الصفحة الرئيسية (Home Tab - أيقونة المنزل): تعرض جميع الكتب
        المرفوعة/المحملة مرتبة حسب أحدث كتاب تم رفعه أو قراءته.
    2.  المفضلة (Favorites Tab - أيقونة القلب): تعرض الكتب التي أضافها المستخدم
        للرغبات.
    3.  الإعدادات (Settings Tab - أيقونة الترس): التحكم بالثيم، تفعيل وضع حماية
        العين، وإعدادات السرعة الافتراضية.
  - Core UI Elements:
      - الزر الدائري العائم (FAB): يظهر في الصفحة الرئيسية مع رمز (+) لرفع ملفات
        PDF أو EPUB محلية.
      - بطاقة الكتاب (Book Card): تعرض صورة الغلاف (المستخرجة أو الافتراضية)،
        اسم الكتاب، شريط التقدم بالنسبة المئوية، وزر القلب للمفضلة.
      - قائمة الضغط المطول (Contextual Modal): تظهر بأسلوب يتطابق مع ثيم التطبيق
        عند الضغط المطول على الكتاب وتتضمن:
          - تثبيت الكتاب في الأعلى (أيقونة الدبوس).
          - إضافة / إزالة من المفضلة (أيقونة القلب).
          - حذف الكتاب (أيقونة حاوية القمامة).

3. Data & State Management

  - Data Models & Attributes:
      - BookModel:
          - id (String - معرف فريد)
          - title (String)
          - filePath (String - مسار الملف بالمساحة الداخلية)
          - fileType (Enum: pdf, epub)
          - coverPath (String? - مسار الغلاف المستخرج)
          - currentPage (Int)
          - totalPages (Int)
          - progressPercentage (Double)
          - isFavorite (Boolean)
          - isPinned (Boolean)
          - createdAt (DateTime)
          - lastReadAt (DateTime)
      - NoteModel:
          - id (String)
          - bookId (String)
          - pageNumber (Int)
          - content (String)
          - createdAt (DateTime)
  - Storage Strategy:
      - استخدام مكتبة Hive أو SQLite لتخزين بيانات الكتب والملاحظات محلتاً.
      - استخدام path_provider لنسخ ملفات الكتب المحملة أو المرفوعة إلى مسار
        التطبيق المحلي الداخلي.
      - استخدام shared_preferences لتخزين تفضيلات الثيم ووضع حماية العين.

4. Feature Specifications

أ. البحث والتحميل من الإنترنت (Archive.org Integration)

  - شريط البحث بالصفحة الرئيسية يتيح الاستعلام مباشرة عبر Internet Archive API.
  - تصفية النتائج:
      - قصر البحث على صيغة PDF فقط واستبعاد صيغ EPUB وغيرها من النتائج عند البحث
        عبر الإنترنت.
      - تطبيق فلتر فلترة محتوى الكبار (NSFW/Adult Filter) برمجياً عبر استبعاد
        الكلمات المفتاحية والموضوعات الحساسة (subject:erotica,
        subject:pornography, إلخ).

ب. قارئ الكتب (Distraction-Free Reader)

  - واجهة قراءة نظيفة خالية من المشتتات.
  - الأدوات والميزات الداخية (متاحة عبر الترس الجانبي أو الشريط الإضافي):
      - التمرير التلقائي (Auto-Scroll): إمكانية تفعيله/إيقافه مع شريط للتحكم
        بالسرعة.
      - وضع حماية العين (Eye Care): زر لتفعيل/إطفاء الفلتر الدافئ.
      - الانتقال السريع للصفحات: إدخال رقم الصفحة أو استخدام أزرار التنقل
        السريع.
      - إدارة الملاحظات والتعليقات: شاشة/قائمة جانبية تعرض جميع الملاحظات
        المدونة على الكتاب مع رقم الصفحة المرفقة، مع إمكانية النقر على الملاحظة
        للانتقال الفوري لصفحتها.
      - حفظ الصفحة: إمكانية حفظ علامة مرجعية (Bookmark) وحفظ آخر صفحة تلقائياً
        أو يدوياً.
      - زر الخروج: العودة السريعة للشاشة الرئيسية.

5. Explicit Constraints & Non-Goals

  - ممنوع استخدام الإيموجي (No Emojis): تعتمد الواجهة بالكامل على أيقونات
    المتجهات (Material / Lucide).
  - الكتب أونلاين: البحث عبر الإنترنت ينزل ملفات PDF فقط ولا يظهر EPUB.
  - السرية والأمان: عدم إظهار أي محتوى غير لائق في نتائج بحث كتب Archive.org.
  - الأوفلاين: القراءة وكتابة الملاحظات تعمل 100% بدون الحاجة لاتصال بالإنترنت.

Micro-Steps Execution Plan

Step 1: UI Foundation, Theme & Navigation

  - إعداد مشروع Flutter وإضافة المكتبات الأساسية (مثل lucide_icons).
  - ضبط الألوان الأساسية (البني الفاتح، البني الداكن، الأصفر الدافئ) وإعداد
    القوالب (Light, Dark, AMOLED).
  - إنشاء هيكل التبويب السفلي (Home, Favorites, Settings) وشريط البحث العلوى.
  - بناء واجهة بطاقة الكتاب مع بيانات وهمية (Mock Data).

Step 2: User Interactions & Local State

  - تنفيذ شريط البحث وواجهة النتائج ببيانات وهمية.
  - تنفيذ قائمة الضغط المطول (Pin, Favorite, Delete) مع القوائم المخصصة بالتصميم
    المطلوب.
  - تنفيذ التبديل بين الشاشات الثلاث (الرئيسية، المفضلة، الإعدادات).
  - إعداد خيارات الثيمات ووضع حماية العين في شاشة الإعدادات.

Step 3: Local Storage & File Management

  - إعداد قاعدة البيانات المحلية (Hive أو SQLite).
  - ربط زر (+) العائم بـ file_picker لاختيار ملفات PDF و EPUB المحلية.
  - تنفيذ منطق نسخ الملف المختار إلى المساحة الداخلية للتطبيق.
  - بناء محرك استخراج أغلفة الكتب تلقائياً وإنشاء غلاف افتراضي لون بني/أصفر في
    حال تعذر الاستخراج.

Step 4: Core Reader Engine (PDF & EPUB)

  - دمج مكتبة عرض PDF ومكتبة عرض EPUB.
  - بناء شاشة القراءة النظيفة الخالية من التشتيت.
  - تنفيذ أداة التمرير التلقائي للكتب مع التحكم بالسرعة.
  - إضافة ميزة تدوين الملاحظات وتخزينها مرتبطة برقم الصفحة وسردها في قائمة
    جانبية مع إمكانية القفز للصفحة.
  - تطبيق الحفظ التلقائي واليدوي لآخر صفحة تم الوصول إليها.

Step 5: Archive.org API Integration & Safety Filtering

  - الربط مع API الخاص بـ archive.org/advancedsearch.php.
  - تطبيق الفلترة البرمجية للنتائج لإظهار صيغ PDF فقط.
  - تطبيق الفلتر الخاص بحظر محتوى الكبار (NSFW Filter).
  - إضافة نظام تنزيل الملفات وإضافتها تلقائياً لمكتبة المستخدم المحلية.
  - المراجعة النهائية واختبار الأداء والألوان.

Operational Delivery Guidelines

1.  الالتزام الدقيق بالخطوات: سيتم تقديم الكود المصدري خطوة بخطوة بناءً على
    خريطة Micro-Steps أعلاه دون القفز للخطوات المستقبلية.
2.  منع الإيموجي: لن يتم استخدام أي رمز إيموجي في الكود أو التصميم، وسيتم
    الاعتماد الكامل على أيقونات المتجهات.
3.  التكامل الذاتي: كل خطوة برمجية ستتضمن كافة الواردات (Imports) وإدارة الحالات
    بشكل محكم.
4.  معالجة الأخطاء: تغليف عمليات التنزيل وقراءة الملفات بكتل try-catch نظيفة
    لضمان استقرار التطبيق.
